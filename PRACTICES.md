# 開発プラクティス集 — ROS 2 / Docker / WSL2

この環境（`Dockerfile` / `compose.yaml` / `run.sh` / `demo.sh` / `ws/`）を組み立てる過程で
判明した制約と、それに対して採った方針をまとめたものです。
README は「使い方」、こちらは「なぜそう作ってあるか」を扱います。
別のプロジェクトで同じ構成を組むときの下敷きになることを想定しています。

各項目は **やったこと → なぜ → 応用するときの注意** の順で書いてあります。

---

## 1. 全体方針

### 1.1 ホストには何もインストールしない

ROS 2 本体・Gazebo・RViz2・依存パッケージはすべてコンテナ内に置き、ホスト側 (WSL2 の Ubuntu)
には Docker しか入れていません。

**なぜ** — ROS は apt で数百パッケージを引き込み、ディストリ (Jazzy / Humble) ごとに
`/opt/ros/<distro>` を占有します。ホストに直接入れると、別バージョンを試すたびに環境が壊れ、
やり直しに OS の再インストールが要る状態になりがちです。
コンテナなら `docker compose down -v` で完全に戻せます。

**注意** — 「消せること」が価値なので、**コンテナ内で `apt install` したものは失われる前提**で扱います。
一時的な調査は `sudo apt install` で構いませんが、恒久的に必要になったと判断した時点で
`Dockerfile` に書き戻すこと。これを怠ると「自分の手元でだけ動く環境」に逆戻りします。

### 1.2 ソースはホスト、ビルドと実行はコンテナ

`ws/` だけをホストと共有し (`./ws:/ws`)、エディタ・git はホスト側、`colcon build` と
`ros2 run` はコンテナ内で実行します。

**なぜ** — エディタや git の設定をコンテナに持ち込むと、コンテナが「もう一つの環境」に育って
1.1 の利点が消えます。共有するのは成果物 (ソース) だけに絞るのが境界として一番安定します。

**注意** — 共有ディレクトリに生成物が出ます (`build/` `install/` `log/` `__pycache__/`)。
`ws/.gitignore` で除外済み。バインドマウント越しのビルドは I/O が遅いので、
巨大なワークスペースでは `build/`・`install/` を名前付きボリュームに逃がす選択肢もあります。

### 1.3 ホストとコンテナで UID を揃える

`Dockerfile` の `ARG UID=1000 / GID=1000` と `compose.yaml` の `args` で、
コンテナ内ユーザ `rosdev` をホストユーザと同じ UID にしています。

**なぜ** — バインドマウントの所有権は UID の数値でしか一致しません。ずれていると、
コンテナが `ws/` に作ったファイルがホストから編集できない (root 所有) 状態になります。

**注意** — Ubuntu 24.04 ベースイメージには UID 1000 の `ubuntu` ユーザが**既にいます**。
`useradd` は「UID 重複」で失敗するので、既存アカウントがいれば `usermod -l` で改名して
枠を再利用する分岐を入れてあります（`Dockerfile` のユーザ作成ブロック）。
ホストの UID が 1000 でない環境に移すときは、`compose.yaml` の `UID`/`GID` と、
`~/.cache` を指す名前付きボリュームのパスの両方を直す必要があります。

---

## 2. コンテナの設計

### 2.1 環境設定は `.bashrc` ではなく `/etc/profile.d` に置き、`.bashrc` から読む

```
/etc/profile.d/10-ros.sh   ← source /opt/ros/jazzy/setup.bash と overlay
~/.bashrc                  ← 上を source + colcon 補完
```

**なぜ** — `.bashrc` は非対話シェルで**冒頭で return する**ため、
`docker compose exec ros bash -lc 'ros2 topic list'` のような自動化呼び出しでは
ROS が source されません。逆に `profile.d` は対話 `bash` (ログインシェルでない) では読まれません。
両方から到達できるように、実体を `profile.d` に置き `.bashrc` がそれを読む形にしています。

**注意** — スクリプトからコンテナ内コマンドを叩くときは `bash -lc`（ログインシェル）を使うこと。
`demo.sh` の `in_container()` がこの形です。`bash -c` だと profile.d が読まれず、
「手で打つと動くのにスクリプトだと `ros2: command not found`」という典型的な症状になります。

### 2.2 overlay の source は存在チェック付きで

```bash
[ -f /ws/install/setup.bash ] && source /ws/install/setup.bash
```

**なぜ** — ワークスペース未ビルドの状態（＝初回起動時）でも、シェルがエラーなく開く必要があります。
`set -e` 下だと `&&` の失敗が終了に繋がるため、`if` 文にするか末尾に `|| true` を足すのが安全です。

### 2.3 名前付きボリュームの所有権は「マウント前に mkdir」で決める

```dockerfile
USER ${USERNAME}
RUN mkdir -p /home/${USERNAME}/.cache      # ← ボリュームが被さる前に作る
```

**なぜ** — Docker は名前付きボリュームを初回マウント時に、**マウント先ディレクトリの所有権と内容を
コピーして**初期化します。マウント先が存在しないと root 所有で作られ、
一般ユーザで動くプロセス（Mesa のシェーダキャッシュなど）が書き込めません。
イメージ側で正しい所有者のディレクトリを先に用意しておけば、ボリュームがそれを引き継ぎます。

### 2.4 `/dev/shm` を明示的に広げる

`shm_size: "2gb"`。Docker の既定は 64MB で、Gazebo・RViz2 はこれを超えます。
症状は「起動直後に理由不明で落ちる」なので、原因に辿り着きにくい部類です。
GUI や大きなポイントクラウドを扱うコンテナでは最初から入れておくのが無難です。

### 2.5 `network_mode: host` を使うなら `extra_hosts` も

host ネットワークではコンテナの `/etc/hosts` に自分のホスト名が入りません。
`sudo` が名前解決に失敗して毎回警告を出すので、
`extra_hosts: ["ros2-jazzy:127.0.0.1"]` で自己解決を足しています。

---

## 3. WSL2 / WSLg 固有の壁

このマシンでは Docker が **snap 版**であることが制約の中心にあります。
snap の confinement により `/usr` 以下をコンテナに bind mount できません。
以下はすべてその回避策です。同じ構成を別マシンに移すなら、まず
`docker version` の提供元（snap / apt / Docker Desktop）を確認してください。

### 3.1 GPU ライブラリはマウントではなくイメージに焼き込む

```bash
# run.sh: ビルド前にホストから退避
cp -u /usr/lib/wsl/lib/{libd3d12.so,libd3d12core.so,libdxcore.so} .wslgpu/
```
```dockerfile
COPY .wslgpu/ /usr/lib/wsl/lib/
ENV LD_LIBRARY_PATH=/usr/lib/wsl/lib
```

**なぜ** — Mesa の d3d12 ドライバがこの 3 つを要求しますが、`/usr/lib/wsl` を
マウントできない（3.冒頭）。ビルドコンテキストに退避してから `COPY` しています。

**注意** — ホスト由来のバイナリをイメージに入れているので、**イメージの可搬性はありません**。
WSL のドライバが更新されたら再ビルドが要ります。`run.sh` が毎回 `cp -u` するのはそのためです。
`.wslgpu/` は `.gitignore` 済み（`.keep` のみ追跡）。

### 3.2 X11 ソケットは `/mnt/wslg/.X11-unix` を直接マウントする

**なぜ** — ホストの `/tmp/.X11-unix` はそれ自体が WSLg のバインドマウントで、
snap 制限下ではコンテナから**空に見えます**。実体側を直接マウントすれば通ります。

```yaml
- /mnt/wslg/.X11-unix:/tmp/.X11-unix
- /mnt/wslg:/mnt/wslg
```

### 3.3 `XDG_RUNTIME_DIR` は 0700 の専用ディレクトリを用意する

Qt は world-writable な `XDG_RUNTIME_DIR` を拒否します。WSLg の
`/mnt/wslg/runtime-dir` は 0777 なので、そのまま指すと Qt アプリ（RViz2 等）が起動しません。
`/run/user/1000` を 0700 で作り、必要なソケット（`wayland-0`, `pulse`）だけを
symlink で繋いでいます。

### 3.4 「動く経路」と「速い経路」を分けて記録する

このマシンの WSLg の Xwayland は DRI3 非対応 (`screen 0 does not appear to be DRI3 capable`) で、
OpenGL は llvmpipe（CPU 実装）に落ちます。**コンテナのせいではなく**、
WSL 上で直接 ROS を動かしても同じです。

**プラクティス** — この種の性能問題は「原因の切り分け結果」と「実測値」をドキュメントに残すこと。
残していないと、後で必ず「Docker が遅いのでは」と疑って同じ調査をやり直します。
GPU パススルー (`/dev/dxg` とドライバストアのマウント) は配線済みなので、
WSL 側が対応すれば設定変更なしで効きます。

---

## 4. ROS 2 の運用

### 4.1 ディスカバリ範囲は明示する

```yaml
ROS_DOMAIN_ID: ${ROS_DOMAIN_ID:-0}
ROS_AUTOMATIC_DISCOVERY_RANGE: SUBNET   # LOCALHOST / SUBNET / OFF
```

**なぜ** — ROS 2 は既定でマルチキャストにより同一サブネットのノードを勝手に見つけます。
これは実機接続では利点ですが、共有ネットワークでは他人のノードと混線します。
既定値に任せず、`SUBNET`（LAN 上の実機と繋ぐ）／`LOCALHOST`（閉じる）／`OFF`（隔離）を
意識的に選び、共有環境では `ROS_DOMAIN_ID` も分けること。

`run.sh` は環境変数を素通しするので `ROS_DOMAIN_ID=42 ./run.sh` で切り替えられます。

### 4.2 DDS 実装は差し替えられる状態にしておく

既定の Fast DDS に加えて `rmw_cyclonedds_cpp` をイメージに入れてあります。
`export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp` で切り替え。

**なぜ** — 「ノードは立っているのにトピックが見えない」系のトラブルは、
DDS 実装を替えると切り分けられることが多いためです。後から apt で入れるのは
（1.1 の通り）失われるので、最初からイメージに含めておきます。

### 4.3 `colcon build --symlink-install` を既定に

Python パッケージはシンボリックリンクになるため、編集が再ビルドなしで反映されます。
反復の速度が変わります。C++ は当然ビルドが要ります。

---

## 5. シミュレーション

### 5.1 題材は「実機と同じインターフェースが出るもの」を選ぶ

turtlesim ではなく TurtleBot3 + Gazebo を使っています。
`/cmd_vel`（速度指令）・`/odom`（オドメトリ）・`/scan`（LiDAR）・`/tf` という、
実機の差動二輪ロボットとまったく同じトピック構成が出てきます。

**なぜ** — ここで書いた制御コードがそのまま実機に載ります。turtlesim は
`/turtle1/cmd_vel` と独自の `Pose` 型で、学んだことの移植先がありません
（比較用に `ws/scripts/draw_square.py` を残してあります）。

### 5.2 上流の launch を「引数で曲げられない」ときは、別ファイルに再構成する

`turtlebot3_gazebo` の launch は `gz sim -g`（GUI クライアント）を必ず起動し、
引数で無効化できません。しかも GUI を kill すると `on_exit_shutdown` で launch 全体が落ちます。

**採った方針** — 上流ファイルを編集・パッチせず、必要な部分
（`gz_sim.launch.py` をサーバのみ `-s` で起動 / robot_state_publisher / spawn）だけを
`IncludeLaunchDescription` で組み直した `ws/launch/tb3_headless.launch.py` を用意。

**なぜ** — `/opt/ros` 以下への直接編集はコンテナ再作成で消え、再現性がなくなります。
上流は読み取り専用として扱い、差分は自分のワークスペース側にファイルとして持つのが原則です。

### 5.3 上流が環境変数に依存しているなら、呼び出し側で必ず設定する

`turtlebot3_gazebo` の launch は `TURTLEBOT3_MODEL` が未設定だと落ちます。
イメージの `ENV` に入れてありますが、それに頼らず

- launch 側: `SetEnvironmentVariable('TURTLEBOT3_MODEL', LaunchConfiguration('model'))`
- シェル側: `MODEL="${TURTLEBOT3_MODEL:-burger}"`

と二重に既定値を持たせています。「どこかで export されているはず」という前提は、
別ターミナル・別ユーザ・スクリプト経由で必ず崩れます。

### 5.4 性能は主観でなく数値で記録する

`ws/scripts/sim_speed.py` は `/clock` を購読して実時間との比 (real-time factor) を出します。

- GUI あり: **RTF 約 0.69**
- ヘッドレス: **RTF 約 0.81**（GUI のコストは約 15%）

**プラクティス** — 「重い気がする」で終わらせず、測る手段そのものをリポジトリに置くこと。
環境を移したとき・設定を変えたときに同じ物差しで比較できます。

---

## 6. ロボット制御コードの書き方

`ws/scripts/robot.py` に入れてある設計判断です。シミュレータ専用ではなく、
実機にも向けられる書き方を意識しています。

### 6.1 メッセージ型はハードコードせず、実行時にグラフから決める

Jazzy で複数のベースが `/cmd_vel` を `Twist` から `TwistStamped` に移行しました。
どちらか決め打ちすると、相手が違うだけで「publish しているのに動かない」状態になります。

```python
for name, types in self.get_topic_names_and_types():
    if name == topic and types:
        return TwistStamped if "TwistStamped" in types[0] else Twist
```

**注意** — ノード起動直後はディスカバリが収束していません。1 回読んで判断すると外すので、
タイムアウト付きでポーリングし、最後まで見つからなければ既定 (`Twist`) にフォールバックします。

### 6.2 センサ購読の QoS は BEST_EFFORT に合わせる

LiDAR・カメラなどのセンサ配信は BEST_EFFORT が通例です。購読側が既定の RELIABLE のままだと
**QoS 不一致で一切データが来ません**（エラーも出ないため原因が分かりにくい）。
`robot.py` は `SENSOR_QOS`（BEST_EFFORT / VOLATILE / KEEP_LAST depth 5）を定義して使っています。

### 6.3 開ループ（時間指定）ではなく閉ループ（センサ指定）で動かす

「0.2 m/s で 5 秒」ではなく「`/odom` を見て 1.0 m 進むまで」。
シミュレータの RTF が 1.0 未満（5.4）である以上、時間ベースの指令は距離とずれます。
実機ならスリップや電圧でずれます。フィードバックで書けば、どちらでも同じコードが使えます。

### 6.4 安全停止は制御ロジックとは別の層に置く

`forward()` は毎ループ `front_distance()` を見て、`STOP_DISTANCE`(0.35 m) を切ったら
指令の残りを無視して止まります。上位のロジック（`wander` など）がどんな指令を出しても、
壁に突っ込むことはありません。

**なぜ** — 「賢い制御が壁を避ける」ではなく「愚かな制御でも壁に当たらない」構造にしておくと、
上位ロジックを気軽に書き換えられます。実機に載せる前提なら必須の分離です。

### 6.5 デッドロックからの脱出経路を用意する

`wander` は最も空いている方位を選んで進みますが、進めなかった (`moved < 0.05`) 回数を数え、
2 回続いたら**ランダムに 90〜170 度**曲がります。
決定的な選択則だけだと、同じ場所で同じ方向を選び続けて永久に抜けられません。
対称性をランダムに壊す退避策を入れておくのが定石です。

### 6.6 トピック名は引数で差し替え可能に

`--cmd-topic` / `--odom-topic` / `--scan-topic`。ロボットが変われば名前空間が変わります
（`/robot1/cmd_vel` など）。既定値を持たせつつ外から変えられるようにしておけば、
コードを触らずに別機体へ向けられます。

---

## 7. 自動化スクリプト（`demo.sh` / `run.sh`）

「手順書に 3 ターミナル」で終わらせず、一発で再現するスクリプトを置いています。
手順書は必ず腐りますが、スクリプトは実行すれば腐っていることが分かります。

### 7.1 バックグラウンド起動は `setsid` で親から切り離す

```bash
docker compose exec -d ros bash -lc "exec setsid <cmd> > /tmp/x.log 2>&1"
```

**なぜ** — `exec -d` で起動したプロセスは exec セッション終了時に道連れになります。
`setsid` で新しいセッションにすると生き残ります。

**注意** — `setsid` は実プログラムを exec するため、コマンドの先頭がシェル組み込みだと使えません。
環境変数を渡すときは `export VAR=x; cmd` ではなく **`env VAR=x cmd`** と書くこと。

### 7.2 `pkill -f` のパターンは 1 文字を `[]` で囲む

```bash
pkill -f "[t]b3_headless.launch"
```

**なぜ** — `pkill -f` はコマンドライン全体に対して照合するため、
このコマンドを実行しているシェル自身のコマンドラインにもパターン文字列が含まれます。
囲まないと、リストの残りを処理する前に自分自身を kill します。

### 7.3 待つときは固定 sleep でなく「条件が満たされるまで」ポーリングする

```bash
for _ in $(seq 30); do
    ros2 topic list | grep -qx /scan && break
    sleep 3
done
```

Gazebo の起動時間はマシンとワールドで大きく変わります。固定 `sleep 60` は
遅いマシンで壊れ、速いマシンでは無駄になります。
**タイムアウト付きの条件待ち**にし、超えたら失敗と判定してログを出します。

### 7.4 失敗したときに原因が見える形で落とす

- 起動前にログファイルを空にする (`: > /tmp/demo_gz.log`)。
  前回の残骸があると「起動していない」が「起動して失敗した」に見えます。
- 条件待ちがタイムアウトしたら `tail -20` でログを出して `exit 1`。

自動化スクリプトの価値の半分は、失敗時に次の一手が分かることです。

### 7.5 `run.sh` は「無ければ作る、あれば繋ぐ」の一枚に

イメージが無ければビルド、コンテナが止まっていれば起動、最後に `exec docker compose exec bash`。
別ターミナルでもう一度叩けば同じコンテナに 2 枚目のシェルが繋がります。
ROS はターミナルを何枚も開く作業なので、この冪等性が効きます。

---

## 8. AI と一緒に環境を作るときの進め方

この環境自体を AI（Claude Code）と対話しながら組み立てた際に、効いた進め方です。

### 8.1 「なぜ」をコードのコメントとして残す

この環境の Dockerfile / compose.yaml のコメントは、ほぼすべてが
**回避策とその理由**です（snap Docker、Qt の 0700 要求、profile.d の非対話シェル問題…）。

**なぜ** — 回避策は、見た目には「不自然なコード」です。理由が併記されていないと、
後から自分でも AI でも「これは要らない」と判断して消し、同じ問題を踏み直します。
AI に作業させるときは特に、**理由のないコードは消される**前提でいた方が安全です。

### 8.2 調査結果は数値と原文エラーで残す

`screen 0 does not appear to be DRI3 capable` / `RTF 0.69` / `ベースイメージ 6.35GB` /
`約 15 分` のように、観測した文字列と数値をそのまま README に書いています。
要約すると再検証できなくなります。

### 8.3 「動作確認済み」のリストを明示的に持つ

README の「動作確認済みの内容」は、AI が作った構成のどこまでを実際に動かして確かめたかの記録です。
生成されたものは、確認していない限り**動く保証がありません**。
確認済み／未確認（この環境では Nav2・SLAM は未インストール、実機 USB 接続は未検証）の線引きを
文書に書いておくと、次に触るときの出発点になります。

### 8.4 権限は最小で明示する

`.claude/settings.local.json` で、確認なしに許可する操作を必要な範囲
（ここでは `Bash(docker image *)`）に限定しています。
毎回の確認を減らしつつ、破壊的な操作は手動確認に残す線引きです。

### 8.5 環境の再現条件を書き出す

「別のマシンで再現する」節で、必要なファイル・不要な生成物・前提
（WSL2 + Ubuntu 24.04 + Docker、ホスト UID 1000）・WSL2 以外での差分を明記しています。
AI に作らせたものは自分の頭の中に構築過程が無いぶん、
**前提条件を文書化しておかないと移植時に手が止まります**。

---

## 9. この環境の未対応事項

意図的に入れていないもの。手を入れるときの起点として。

- **Nav2 / SLAM** — 未インストール。`Dockerfile` に
  `ros-jazzy-nav2-bringup ros-jazzy-slam-toolbox ros-jazzy-turtlebot3-navigation2` を追記して再ビルド。
- **実機 USB/シリアル** — 配線は未検証。Windows 側で `usbipd attach --wsl --busid <BUSID>` した上で、
  `compose.yaml` の `devices:` に**そのデバイスだけ**追記する（`/dev` 全体は渡さない）。
- **GPU アクセラレーション** — 設定済みだが WSLg の DRI3 非対応により未発効（3.4）。
- **`ws/src` のパッケージ** — 現状は空。制御コードは `ws/scripts/` に素の Python で置いてあり、
  ament パッケージ化はしていない。
